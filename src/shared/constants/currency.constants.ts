/**
 * Configuración global de moneda para la aplicación.
 * Todos los montos (órdenes, gastos, reportes, POS) usan esta configuración.
 *
 * Formato de números: coma como separador de miles, punto como decimal (ej. 3,000.20).
 */

/** Código ISO de la moneda (ej. MXN, USD, CLP) */
export const CURRENCY_CODE = 'MXN';

/** Locale para formateo numérico (en-US = miles con coma, decimal con punto) */
export const CURRENCY_LOCALE = 'en-US';
