# API base URL (sin barra final)
# Ejemplo local: http://localhost:3000
# Ejemplo QA: https://api-qa.tudominio.com
VITE_API_BASE_URL=

# WebSocket URL (mismo origen que la API en la mayoría de casos)
# Ejemplo local: http://localhost:3000
VITE_WS_URL=
